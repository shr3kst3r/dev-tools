# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.ty_cache/

# uv / virtualenv
.venv/

# OS / editor
.DS_Store
*.swp

# local skill config — belongs in ~/.dev-tools.env, never in the repo
dev-tools.env
.dev-tools.env

# ephemeral rpi / adr-rpi artifacts (research.md, plan.md, implementation.md)
.rpi/
.adr-rpi/
.context/
