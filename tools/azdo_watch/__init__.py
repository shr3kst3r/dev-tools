"""azdo-watch — a live dashboard of an Azure DevOps project's pipelines.

Modeled on `tools/airflow_watch`: the same investigation loop (list → drill →
log), the same Textual windowing, the same injected-callable seams.
"""
